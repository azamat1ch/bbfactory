# Factory card implementation

- Kept typography commit ab6ec254d intact in ancestry and retained its existing base styles. The detail body uses the existing text-sm token; compact requirement rows use aligned CSS grid columns and a narrow-container layout with natural wrapping.
- Removed duplicate proposed-team/native-assignment panels. Scope stays visible; notes, approval history, spec history and delivery snapshot details remain collapsed. Empty notes/findings disappear.
- Replaced the duplicate human-review list/form with selection on the paginated requirements and one Approve / Approve selected action. Displayed selected IDs persist across filters/pages; version/content changes clear selection. Chat remains the detailed feedback route, with persisted rationale/provenance shown in approval history.
- Implemented the records worker's published contract: factoryApproveDelivery, factoryResumeTask, executionRunning, approvals, deliveries, verificationMethods and invalidatedEvidenceIds. These must be integrated with the backend changes before compiling or running UI tests. No shared schema or package configuration was edited.
- Existing local coverage derivation now requires all specified methods and ignores invalidated records. Cancellation alone no longer marks passing rows stale. Historical delivery remains distinct from current coverage.
- Verification performed: Prettier parsed/formatted the six changed UI files; git diff --check passed. No builds, test suites, installs or typechecks were run because the lead owns the single heavy-check slot.
- Proposed integration checks: run app.test.tsx through the resulting package's Turbo test task with one Vitest worker, then the affected package typecheck. Browser inspection still needed at narrow and wide card widths with 100 rows, mixed methods, long/unbroken text, selected rows across pages, and historical delivery plus stale current coverage. The DOM structure test does not establish visual wrapping or responsive usability.
- No acceptance claimed. Lead owns rename integration, combined verification and review.
