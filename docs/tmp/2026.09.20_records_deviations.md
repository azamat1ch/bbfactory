# Records implementation deviations and handover

- Preserved the existing JSON-in-SQLite task store and immutable artifact tables. Additive schema defaults need no SQL migration; no Team tables or execution adapter contract changed.
- Published the API contract at `/tmp/factory-implementation-thr_5didxi6rn7/records-contract.md`; the initially requested personal workspace path was read-only. Lead and UI were notified.
- Requirements accept combined verification methods; an empty array retains legacy criterion meaning. All declared methods qualify independently. An automated route needs a real required check or useful review instructions at start; instructions alone never pass verification.
- Whole-delivery/selected approval records are independent of coverage, with exact version/content/scope, explicit actor, origin and optional rationale. Chat origin requires a source reference. Human-method requirements additionally receive judgments. No authentication/identity infrastructure was added: existing trusted RPC callers remain responsible for forwarding actual explicit human decisions, not invented attestations.
- Delivery records preserve historical content/spec/status after later changes. Export/import transfers the current spec meaning only into a fresh draft; original revisions/evidence remain in the source task.
- Observed content transitions permanently invalidate earlier evidence IDs, including observed away-and-back transitions. Changes entirely between observations cannot be detected by this polling/content-fingerprint architecture. UI must exclude `invalidatedEvidenceIds` from coverage.
- Cancellation rejects idle execution; existing archive lifecycle cleanup remains allowed. Stop does not invalidate evidence. Legacy resume requires settled native assignments and launches nothing. Running check snapshots can be read without waiting behind the verification lock.
- Added focused backend regressions, but no tests, builds, installs or typechecks ran: lead retains the single heavy-check slot. Prettier parsing/formatting and `git diff --check` passed. An attempted lightweight TypeScript transpile was unavailable because this clone has no installed TypeScript module; no install attempted.
- Lead integration must update canonical product/CLI guidance and merge these OLD-path files before package relocation. Proposed serial checks: `pnpm exec turbo run test typecheck --filter=bb-plugin-bbfactory` before relocation, or the consolidated package's actual name after relocation. Tests require the normal installed workspace and native SQLite dependency. No acceptance claimed.

## Revision provenance follow-up

- v1 exports now include optional `source: {taskId, specVersion, exportedAt}`. Imports persist that supplied metadata as an immutable `import-source` artifact and a visible historical note in the same transaction as the fresh draft. Metadata is attribution supplied by the bundle, not verified identity or acceptance. Old v1 bundles without source remain valid. No revision/evidence synchronization was added.
- Added a focused regression for revision 2 provenance, restart persistence, fresh unverified imports, legacy bundles and unchanged source acceptance. Heavy checks remain reserved for the lead.
