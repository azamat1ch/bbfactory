# Provider audit deviations journal — 2026-09-19

Scope: `docs/product/provider-support.md` only. Worktree `provider-audit`,
base `223015a69`. No runtime source edits, commits, pushes or config changes.

| # | Expected | Observed | Evidence | Action | Remaining risk |
| --- | --- | --- | --- | --- | --- |
| 1 | GLM/Z.ai needs its own harness/subscription path | GLM model families are already selectable through the local Devin account (`devin acp` `session/new` configOptions lists `glm-5-2*`, `glm-5-3*`, `glm-5-3-flash*`, `fusion-*-sidekick-glm-5-2`; `devin models list` shows them with per-token prices) | Live ACP probe + `devin models list` on this machine | Documented GLM as a model on existing harnesses; direct Z.ai plan left unverified | Devin-side quota unit/cost attribution for GLM models unknown |
| 2 | `devin` is not ACP-capable without the pending native provider | `devin acp` answers `initialize` today: `loadSession`, session `list`/`delete`, `authMethods: devin-browser`, modes accept-edits/smart/ask/plan/bypass, 385-entry model config option | Read-only JSON-RPC probe over stdio (no `session/prompt` sent, quota impact not measured) | Noted `customAgents` fallback; native provider remains pending review on `codex/native-devin` | Probe is not a BB-driven run; steer/resume through the real bridge unverified |
| 3 | ACP `steerMode: "queue"` means queued until next prompt boundary | Implementation cancels the in-flight `session/prompt` and re-sends the queued input inside the same BB turn | `packages/provider-bridge-acp/src/bridge/bridge.ts` `turn/steer` → `requestSteerCancel` → `session/cancel` | Flagged in matrix; supervisors must not assume mid-turn injection for ACP providers | Doc/handshake wording may mislead future consumers |
| 4 | All shipped ACP agents report usage | Only `acp-cursor` declares `providerUsage`; others return `supported: false` | `known-agents.ts`, `getAcpProviderUsage` | Matrix marks OpenCode/Devin-ACP usage unsupported | Unsupported sources may be absent; absence must not be interpreted as zero quota |
| 5 | Local harness coverage broader | `claude`, `cursor-agent` not on PATH; `opencode` installed but 0 credentials; `hermes` binary broken | `command -v`, `codex login status`, `devin auth status`, `opencode auth list` | Matrix records local availability honestly | Claude/Cursor cells rest on source only, no local run |
| 6 | Cursor usage joins the dedup scheme | Cursor usage never sets `accountKey` → cannot dedupe a Cursor account across machines/pools | `readCursorUsage`/`normalizeUsage` in `provider-bridge-acp` provider-maintenance | Listed as missing piece; proposed capacity record flags it | Cross-machine double counting possible if Cursor is pooled later |
| 7 | Codex usage works for any login | ChatGPT logins only; API-key auth returns an explicit no-subscription-limits state | `getCodexProviderUsage` credentials switch | Documented | None |
| 8 | Claude API-key auth would still show subscription state | OAuth-only reader: API-key-only setups report `unauthenticated` | `claudeCredentialsSchema` requires `claudeAiOauth` | Documented, incl. Z.ai-endpoint caveat | A Claude install routed to a third-party endpoint misleads usage display |

## Integration review corrections

A fresh Astra reviewed the source independently. The original audit overstated
ACP filesystem confinement, model rejection, GLM/Z.ai subscription readiness and
the proposed capacity record equivalence. The integrated matrix narrows those
claims, reuses the actual native usage schemas and explains ACP resume/steer
limits. Catalog/auth/version probes lacked saved raw receipts, so they remain
reported observations; no measured quota-cost or successful BB execution claim
is made. Original exploratory assumptions in the table above are historical
observations, not product decisions.
