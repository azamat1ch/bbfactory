# Parity-map deviations — 2026-09-19

Owner: independent Astra assessment. Assigned worktree: `/home/azamat/projects/subscription-team-workspace/worktrees/parity-map`. Base: `223015a69992d6dfad9c5a9cdb5b2b4e40a21a48`. Deliverable: [native parity map](../product/parity-map.md). No existing source or documentation was edited; no commits, pushes, paid calls or additional agents.

| Expected | Observed | Evidence | Action | Remaining risk |
| --- | --- | --- | --- | --- |
| Brief describes a Devin worker. | Parent explicitly reassigned this analysis to Astra with unchanged artifact/scope rules. | Parent task assignment; brief `/home/azamat/projects/subscription-team-workspace/briefs/parity-map.txt`. | Used Astra identity, base-only inspection, same assigned files. | Independent reviewer still needs to inspect artifact; author identity does not establish correctness. |
| Native workflow persistence may supply substantial execution equivalence. | Worker environment is reused; stop result is discarded; staging helper can intentionally stream; product orchestration remains incomplete. | `plugins/workflows/src/service.ts` (`runAgentCall`, `stopChild`), `runtime.test.ts` streaming-pipeline test. | Kept writer isolation/confirmed stop/stage barriers as required adaptations; no measured credit. | First implementation slice and concurrent worker changes were not inspected or counted. |
| Inventory says unknown mode fails and final answer precedes terminal metadata. | Upstream accepts `review-*` aliases; final artifact copies precede metadata but registry final file follows it. | Pinned upstream `scripts/lib/mode_policy.py:canonical_mode`, `scripts/lib/steer/supervisor.py:_finalize`. | Recorded precise interpretation; proposed explicit native mode policy and durable final-availability contract. | Full runtime conformance still needs failure/race tests. |
| Generic native profile configuration might fail closed. | Invalid custom ACP JSON is warned and ignored. | `plugins/provider-acp/src/configured-agents.ts:resolveConfiguredAcpAgents`. | CFG-01/07 remain adaptations; selected product profile must not silently fall back. | Product-specific policy boundary not implemented in this assignment. |
| Existing provider and thread APIs might satisfy broad inventory categories. | Native thread history is not twelve-store read-only inspection; generic ACP does not prove Devin/Grok semantics; usage normalization does not prove cross-harness pool identity. | Native evidence groups H/A/U and upstream sessions/quota/adapters, linked in parity map. | Retained all useful history/quota/provider requirements; four exclusions only for distinct implementation/scope details. | Proposed 131-row denominator and optional classification require independent approval. |
| A system `python` command could run read-only document validation. | `python` is absent; `python3` is available. | Initial command exited 127; repeated with `python3` succeeded. | Used installed `python3`; no installation or environment change. | None for document validation. |
| Assigned journal parent directory exists. | `docs/tmp` was absent; first file write failed. | Shell file-not-found response. | Created only the assigned journal directory and retried. | None; no existing file was overwritten. |

## Additional user clarification incorporated

Expected: a behavior map may read as predominantly new runtime implementation. Observed: the user clarified that most of Pragmatic Orchestration should ship almost verbatim as native skill/reference/prompt guidance, with Porch calls replaced where necessary. Evidence: parent relay of the user's exact clarification during this assessment. Action: added G/T/I delivery layers to all 135 rows, an actual upstream asset migration matrix, native call ledger and P-S skill packaging lane; retained runtime findings and denominator proposal. Remaining risk: no skill bundle was authored here, and actual preservation must be reviewed through a section-by-section source diff.

A deeper native-surface check found existing SDK `threads.wait` and CLI `bb thread wait`, including bounded status/event waits. ORCH-11 and the call ledger explicitly reuse them; grouped first-terminal waiting and product final collection remain adaptations/gaps. This corrects an earlier draft's overly broad wording without changing the row disposition.

## Verification record

- Initial `git status --short` empty; base commit verified. Upstream was clean at `061bcd6b58a596bc0e1e5403c6aa5c0bdb195ae5`.
- Mechanical row verification: 135 unique IDs exactly equal to `coverage.md`; RUN 32, ORCH 25, REV 19, SQ 16, CFG 12, REC 19, BND 12.
- Dispositions: 63 adaptation-required, 5 native-equivalent candidates, 63 new-required, 4 proposed exclusions. Gates: 121 required, 10 optional, 4 excluded; proposed threshold 125/131, all required mandatory.
- Verified 50 local Markdown link targets and 140 pinned upstream source targets against actual checkout paths; every table row has valid column count. No network availability claim.
- `git diff --check` passed. Because files are new/untracked, separately checked their trailing whitespace/conflict markers and allowed changed-path set.
- No runtime tests/builds/live provider calls were run. All behavior acceptance scenarios are proposed; existing test files are referenced as harnesses, not passing parity evidence.
