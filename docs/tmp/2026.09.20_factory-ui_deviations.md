# Factory living-spec UI deviations — 2026-09-20

Lane: UI worker, isolated worktree `thr_2u63bi6t4p-1`. Scope: `plugins/bbfactory/src/app*.tsx`, `app.css`, `app-status.ts`, `app-review.tsx`, `app.test.tsx`. Frozen contract: `/tmp/factory-living-spec/task-contract.md` (lost to `/tmp` clear on reboot; canonical `shared.ts` copied from backend worktree `thr_qu2fas2vx4-1` for typechecking only — deliberately left uncommitted per lead instruction).

| Expected | Observed / Decision | Remaining risk |
| --- | --- | --- |
| Verify button verifies acceptance | Renamed to "Run checks"; rendered only when phase=active, checks exist, not stopRequested/archived. `factoryVerifyTask` executes linked checks only; agent review is a separate composer request to the lead with requirement IDs/context — no pretend dispatcher. | None |
| Per-requirement human acceptance forms | Replaced by one grouped Human review panel: pending-only checklist with reviewInstructions + artifact links, single notes field, explicit "Accept N selected" (optional notes) and "Request changes" (notes required) via `factoryRecordJudgments`. No attestation checkbox; the button is the attestation. Accepted history collapsed under "Accepted (n)". | Bulk RPC rejects atomically; UI sends only pending human IDs. |
| Status truthfulness | `requirementStatus` mirrors backend evaluate: filter records to current specVersion + observed fingerprint (+ canonicalPath for evidence) first, then `.at(-1)`; latest current record decides. Reverting content re-validates old evidence. Required unresolved finding → failed. | If backend currency differs (e.g., ignores canonicalPath), statuses may disagree — needs conformance test once backend lands. |
| Green suppression | When task is draft, stopRequested, or archived, a computed "accepted" degrades to "stale"; review/judgment/check actions hidden or disabled. Draft shows Start + target environment only. | None |
| One content-unavailable explanation | Single status line under the goal covers disconnect, action failure, or incomplete capture; no per-row fingerprint/warning spam. | None |
| Notes/artifacts | Read-only versioned list with kind badges; conclusions get a salient border; artifact refs render as `UrlLink` only for http(s), else plain mono text. `factoryRecordNote` add-form deferred (contract marks it optional). | Add-note form unimplemented — CLI/tool path exists. |
| Legacy single `factoryRecordJudgment` | UI uses only the bulk RPC; single RPC remains available through RPC/CLI compatibility. Neither human judgment RPC is exposed as an agent tool. | None |
| Spec history | Collapsed; each version shows changeReason + computed delta (req/check/scenario/team/brief add-remove-edit counts), expandable for full spec. | Delta is a summary, not a diff view. |
| Verification | Not run — user RAM constraint; lead owns bounded serialized verification. `pnpm exec turbo run test typecheck --filter=bb-plugin-bbfactory` pending. | app.test.tsx assertions unverified against the harness; expect minor text/role fixes on first run. |
